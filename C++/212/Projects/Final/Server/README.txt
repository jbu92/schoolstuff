Works

	- SMTP Conversation
		- Client can send message to server (local user)
		- Client can send message to server (remote user)
	- Forwarding, actually talks to other servers

Doesn't work
	- MRP (Message Retreival Protocol)